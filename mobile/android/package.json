{
  "name": "@sidra/android-experiment",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.3",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "cap": "cap",
    "sync": "cap sync android",
    "open": "cap open android",
    "build": "cap sync android && cd android && ./gradlew assembleDebug",
    "run": "cap run android",
    "doctor": "cap doctor android"
  },
  "dependencies": {
    "@capacitor/android": "9.0.0-alpha.3",
    "@capacitor/core": "9.0.0-alpha.3"
  },
  "devDependencies": {
    "@capacitor/cli": "9.0.0-alpha.3",
    "typescript": "^6.0.0"
  }
}
